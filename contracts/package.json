{
  "name": "supply-chain-contracts",
  "version": "1.0.0",
  "description": "Supply Chain Smart Contracts",
  "scripts": {
    "hardhat": "hardhat",
    "node": "hardhat node",
    "node:persistent": "ganache --database.dbPath ./ganache-db --wallet.mnemonic \"test test test test test test test test test test test junk\" --wallet.totalAccounts 20 --wallet.defaultBalance 10000 --chain.chainId 31337 --server.host 127.0.0.1 --server.port 8545",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:local:force": "FORCE_DEPLOY=1 hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ganache": "^7.9.2",
    "hardhat": "^2.20.0"
  }
}
